import { UserActions, UserActionsTypes } from './actions'

export const reducer = (state: any, action: UserActions): any => {

    switch (action.type) {
        // case UserActionsTypes.any :
        // return {
        //     ...state,

        // }
        default:
            return state;
    }


}